name = "codequest-tutor"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

# Set the account_id here, or pass CLOUDFLARE_ACCOUNT_ID to wrangler-action
# in CI instead (see .github/workflows/deploy-worker.yml).
# account_id = "YOUR_CLOUDFLARE_ACCOUNT_ID"
