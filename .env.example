# Copy this to .env.local (gitignored) for local development.
# Points the app at the tutor worker running via `wrangler dev` in worker/.
# Leave unset in production if the worker isn't deployed yet — the app
# falls back to static hints automatically.
VITE_TUTOR_API_URL=http://127.0.0.1:8787
